#include "main.h"
#include <stdlib.h>
#include <string.h>
/**
 * _strlen - function of task 2
 * @s: variable of char
 * Return: int value will returned
 */
int _strlen(char *s)
{
	return (strlen(s));
}

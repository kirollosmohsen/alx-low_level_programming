#include <stdio.h>
#include "main.h"
/**
 * factorial - jhddddjdjd
 * @n: oidiudiuidu
 * Return: kjdkjdkd
 */
int factorial(int n)
{
long int sum = 1;
long int i = 0;
if (n < 0)
{
return (-1);
}
else if (n == 0)
{
return (1);
}
else 
{
for (i = 1; i <= n; i++)
{
sum =+ (sum * i); 
}
}
return sum;
}
